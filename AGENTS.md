# Agent Guidelines & Engineering Rules — The Picture Square

## Parallax & Motion Rules
- Scroll parallax is allowed on framed feature images via ParallaxFrame only. Never inside masonry galleries. Never more than 6 instances on one route.
- Under `prefers-reduced-motion`, all parallax effects must collapse cleanly without motion transforms.
